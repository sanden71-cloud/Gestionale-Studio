# Dati sensibili - non committare (locale senza sync)
auth/users.json
auth/config.json
# I file auth/users.enc e auth/config.enc (cifrati con VLEKT_SECRET_KEY) si possono committare per sync locale ↔ deploy.

# Dati utenti (opzionale: se vuoi tenere i dati fuori dal repo)
# data/

# Python
__pycache__/
*.pyc
.venv/
venv/

# macOS
.DS_Store

# IDE
.cursor/
